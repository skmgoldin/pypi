# The hidden.computer PyPi Registry

A private PyPi registry accessible at https://pypi.hidden.computer. When connected to a Kubernetes cluster, the entire chart can be deployed using `apply-all.sh`.
